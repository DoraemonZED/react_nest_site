/** 用户性别 */
export enum UserGender {
  MAN = 1, // 男
  WUMAN = 2, // 女
  UNKONW = 0 // 未知
}

export enum UserType {
  SUPER_ADMIN = 0, // 超级管理员
  ORDINARY_USER = 1 // 普通用户
}

export enum StatusValue {
  FORBIDDEN = 0, // 禁用
  NORMAL = 1 // 正常使用
}